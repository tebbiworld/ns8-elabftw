#
# Restic include patterns for the eLabFTW module backup (paths relative to /srv).
# The database comes as a dump (module-dump-state); the MySQL volume and the
# PHP cache are not included.
#
state/environment
state/passwords.env
state/elabftw.env
state/elabftw.sql
volumes/elabftw-uploads
volumes/elabftw-exports
