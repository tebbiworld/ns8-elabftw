#!/usr/bin/env python3

#
# Copyright (C) 2026 tebbi
# SPDX-License-Identifier: GPL-3.0-or-later
#

# Re-run configure-module with the restored settings: writes the env file,
# the Traefik route, starts the pod and re-applies the LDAP configuration.

import os

import agent

env = os.environ
data = {
    "host": env["TRAEFIK_HOST"],
    "http2https": env.get("TRAEFIK_HTTP2HTTPS", "True") == "True",
    "timezone": env.get("TZ", "UTC"),
    "max_upload_mb": int(env.get("MAX_UPLOAD_MB", "100")),
    "ldap_enabled": env.get("LDAP_ENABLED", "False") == "True",
    "ldap_url": env.get("LDAP_URL", ""),
    "ldap_base_dn": env.get("LDAP_BASE_DN", ""),
    "ldap_bind_dn": env.get("LDAP_BIND_DN", ""),
    "ldap_user_attribute": env.get("LDAP_USER_ATTRIBUTE", "sAMAccountName"),
    "ldap_mail_attribute": env.get("LDAP_MAIL_ATTRIBUTE", "mail"),
    "ldap_firstname_attribute": env.get("LDAP_FIRSTNAME_ATTRIBUTE", "givenName"),
    "ldap_lastname_attribute": env.get("LDAP_LASTNAME_ATTRIBUTE", "sn"),
    "ldap_validate_cert": env.get("LDAP_VALIDATE_CERT", "false") == "true",
}
response = agent.tasks.run(agent_id=env["AGENT_ID"], action="configure-module", data=data)
agent.assert_exp(response["exit_code"] == 0, "The configure-module subtask failed!")
